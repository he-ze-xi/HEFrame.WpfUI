<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:HEFrame.Styles.Controls" xmlns:sys="http://schemas.microsoft.com/netfx/2009/xaml/presentation" xmlns:system="clr-namespace:System;assembly=mscorlib">
    <system:Double x:Key="PasswordBoxWidth">120</system:Double>
    <system:Double x:Key="PasswordBoxHeight">25</system:Double>
    <sys:CornerRadius x:Key="PasswordBoxBorderCorner">3</sys:CornerRadius>
    <system:Double x:Key="PasswordBoxToggleButtonWidth">15</system:Double>
    <system:Double x:Key="PasswordBoxToggleButtonHeight">8</system:Double>

    <Style x:Key="HEPasswordBoxStyle" TargetType="controls:HEPasswordBox">
        <Setter Property="Background" Value="{DynamicResource DarkBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DarkBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource LightForeground}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource PrimaryPressed}" />
        <Setter Property="MinHeight" Value="25" />
        <Setter Property="FontSize" Value="{DynamicResource HEUIFontSize}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Padding" Value="3,0,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:HEPasswordBox">
                    <Grid x:Name="grid">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{DynamicResource PasswordBoxBorderCorner}" IsHitTestVisible="True" SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="PART_WaterText" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Opacity="0" Text="{TemplateBinding WaterMark}" />
                                <PasswordBox x:Name="PART_PasswordBox" Padding="{TemplateBinding Padding}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" Background="{DynamicResource TransparentBackground}" BorderBrush="{DynamicResource TransparentBackground}" BorderThickness="0" CaretBrush="{TemplateBinding CaretBrush}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" PasswordChar="{TemplateBinding PasswordChar}" Style="{x:Null}">
                                    <PasswordBox.Template>
                                        <ControlTemplate>
                                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=controls:HEPasswordBox}, Path=CornerRadius}" SnapsToDevicePixels="True">
                                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
                                            </Border>
                                        </ControlTemplate>
                                    </PasswordBox.Template>
                                </PasswordBox>
                                <controls:HEScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" IsHitTestVisible="False" VerticalScrollBarVisibility="Hidden" Visibility="Hidden" />
                                <ToggleButton x:Name="PART_ToggleButton" Grid.Column="1" Width="{DynamicResource PasswordBoxToggleButtonWidth}" Height="{DynamicResource PasswordBoxToggleButtonHeight}" Margin="5,0" Cursor="Hand" Focusable="False" IsChecked="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsShowPassword}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Grid Background="Transparent">
                                                <Path x:Name="icon" Data="{DynamicResource HiddenPassowordEyePath}" Fill="{Binding RelativeSource={RelativeSource AncestorType=controls:HEPasswordBox}, Path=Foreground}" Stretch="Fill" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="true">
                                                    <Setter TargetName="icon" Property="Data" Value="{DynamicResource VisiblePassowordEyePath}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </Grid>
                        </Border>

                        <Border x:Name="PART_ThicknessBorder" Background="{DynamicResource TransparentBackground}" BorderBrush="{DynamicResource LightBackground}" BorderThickness="0,0,0,1" CornerRadius="{DynamicResource PasswordBoxBorderCorner}" IsHitTestVisible="False" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsShowPassword" Value="True">
                            <Setter TargetName="PART_PasswordBox" Property="IsHitTestVisible" Value="False" />
                            <Setter TargetName="PART_PasswordBox" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_ContentHost" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_ContentHost" Property="IsHitTestVisible" Value="true" />
                        </Trigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Text}" Value="">
                            <Setter TargetName="PART_WaterText" Property="Opacity" Value="0.5" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Text}" Value="{x:Null}">
                            <Setter TargetName="PART_WaterText" Property="Opacity" Value="0.5" />
                        </DataTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="grid" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="PART_ThicknessBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBackground}" />
                            <Setter TargetName="PART_ThicknessBorder" Property="BorderThickness" Value="0,0,0,2" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="true">
                            <Setter TargetName="PART_ThicknessBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBackground}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="PART_ThicknessBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBackground}" />
                            <Setter TargetName="PART_ThicknessBorder" Property="BorderThickness" Value="0,0,0,2" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="False" />
                                <Condition Property="IsKeyboardFocused" Value="False" />
                                <Condition Property="IsFocused" Value="False" />
                                <Condition Property="ThemeType" Value="Light" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_ThicknessBorder" Property="BorderBrush" Value="{DynamicResource DarkBackground}" />
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="ThemeType" Value="Dark">
                <Setter Property="Background" Value="{DynamicResource DarkBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource DarkBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource LightForeground}" />
                <Setter Property="CaretBrush" Value="{DynamicResource LightForeground}" />
            </Trigger>
            <Trigger Property="ThemeType" Value="Light">
                <Setter Property="Background" Value="{DynamicResource LightBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource LightBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource DarkForeground}" />
                <Setter Property="CaretBrush" Value="{DynamicResource DarkForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style BasedOn="{StaticResource HEPasswordBoxStyle}" TargetType="{x:Type controls:HEPasswordBox}" />
</ResourceDictionary>