<UserControl x:Class="HEFrameApp.Views.TransitionAnimationView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:HEFrameApp.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="clr-namespace:HEFrame.Styles.Controls;assembly=HEFrame.Styles" d:DesignHeight="450" d:DesignWidth="800" mc:Ignorable="d">
    <ui:HEScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Margin="15,10" VerticalAlignment="Center" FontSize="14" Foreground="{StaticResource DarkForeground}" Text="过渡动画" />
            <ui:HEToggleButton x:Name="AnimationControl" Grid.Row="1" Margin="15,10" ThemeType="Light" />
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <ui:HETransitionAnimation Grid.Row="0" Grid.Column="0" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="ZoomIn">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="缩放进入" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="0" Grid.Column="1" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="ZoomOut">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="缩放移出" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="0" Grid.Column="2" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="GradientIn">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="渐变进入" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="0" Grid.Column="3" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="GradientOut">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="渐变移出" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="1" Grid.Column="0" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideFromLeft">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="左侧滑入" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="1" Grid.Column="1" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideToLeft">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="左侧滑出" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="1" Grid.Column="2" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideFromRight">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="右侧滑入" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="1" Grid.Column="3" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideToRight">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="右侧滑出" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="2" Grid.Column="0" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideFromTop">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="上侧滑入" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="2" Grid.Column="1" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideToTop">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="上侧滑出" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="2" Grid.Column="2" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideFromBottom">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="下侧滑入" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="2" Grid.Column="3" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="SlideToBottom">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="下侧滑出" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="3" Grid.Column="0" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="RotateIn">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="旋转进入" />
                    </Border>
                </ui:HETransitionAnimation>
                <ui:HETransitionAnimation Grid.Row="3" Grid.Column="1" IsStartAnimation="{Binding ElementName=AnimationControl, Path=IsChecked}" TransitionType="RotateOut">
                    <Border Width="80" Height="80" Background="{DynamicResource PrimaryBackground}" CornerRadius="80">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="旋转移出" />
                    </Border>
                </ui:HETransitionAnimation>
            </Grid>
        </Grid>
    </ui:HEScrollViewer>
</UserControl>
